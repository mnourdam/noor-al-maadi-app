<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ platform splash attributes. The unqualified AndroidX
         attributes remain in values/styles.xml for compatibility; these
         explicit platform attrs ensure the system cold-start frame resolves
         the transparent gold foreground instead of falling back to the
         application launcher bitmap. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_splash_icon</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>