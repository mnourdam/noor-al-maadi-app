<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>

    <!--
        Cold-start theme (AndroidX core-splashscreen backport for API 24–30).
        On API 31+ the values-v31 override takes precedence with the platform
        attributes prefixed with `android:`. The animated icon is a plain
        transparent vector (ic_splash_icon) sized to Google's 72dp/108dp inner
        safe zone so no OEM mask draws a plate behind the mark.
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_icon</item>
        <!-- Belt-and-suspenders: force transparent icon plate so no OEM
             backport draws a rounded-square/circle behind the mark. -->
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <!-- Fallback windowBackground for the cold-start frame if the
             splash attrs ever fail to resolve — the OS shows a flat navy
             frame instead of the launcher icon. -->
        <item name="android:windowBackground">@color/splash_background</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>
