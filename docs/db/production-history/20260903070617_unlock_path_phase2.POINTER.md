# Production migration 20260903070617 — pointer record (no local copy)

- Version: `20260903070617`
- Length: 14983 chars, md5 `08cf6c7b2fb46147731f2a1048c066a9`
- First line: `-- Phase 2: set-wise / index-friendly rewrite of the story-list unlock path.`
- Contents: Phase 2 rewrite of `_uuid_or_null_v2`, `_eval_unlock_node_v2`,
  `_eval_unlock_prepared_v2` and the story-list unlock path (signatures,
  return shapes and semantics unchanged).

No local verbatim copy is stored, deliberately: a hand-copied 15 KB body could
introduce transcription drift, and the authoritative verbatim text lives in
production migration history. Retrieve it read-only with:

```sql
select statements[1]
  from supabase_migrations.schema_migrations
 where version = '20260903070617';
```

This migration is ALREADY APPLIED in production. Never execute it again.
